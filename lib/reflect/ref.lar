//Ref相关代码，可以用Ref对象保存一个左值的ref引用，主要用于反射调用方法时候的参数传入
//理论上用这里的接口可以实现类似指针的东东，但larva不推荐这样做，因此这里的Ref对象没有做get/set之类的方法，如果需要类似功能可以用其他方式实现

//函数传参的ref引用封装后的对象
public native class Ref
{
}

//将参数的ref形式封装为Ref对象，larva没有直接保存ref的语法，相关泛型代码需要由编译器自己生成
public Ref make_ref<T>(ref T t)
{
    //编译器生成代码，用t的引用（go层面就是指针）构建Ref对象
}
